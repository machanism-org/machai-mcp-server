# machai-mcp-server
A Maven plugin that integrates a Machai Model Context Protocol (MCP) server directly into Maven builds. Supports stateless/streamable HTTP transports, aggregator goals for multi-module reactors, and seamless AI tool exposure without a separate launcher.
