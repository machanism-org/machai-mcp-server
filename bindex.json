{
  "$schema": "https://raw.githubusercontent.com/machanism-org/machai/refs/heads/main/bindex-core/src/main/resources/schema/bindex-schema-v2.json",
  "id": "org.machanism.machai:machai-mcp-server:1.4.1",
  "name": "org.machanism.machai:machai-mcp-server",
  "version": "1.4.1",
  "location": {
    "repositoryType": "Maven",
    "repositoryUrl": "https://central.sonatype.com/artifact/org.machanism.machai/machai-mcp-server",
    "coordinates": {
      "group": "org.machanism.machai",
      "artifactId": "machai-mcp-server",
      "version": "1.4.1",
      "extension": "jar"
    }
  },
  "description": "Machai MCP Server is a Java 17 command-line server for exposing Machai functional tools and prompts through the Model Context Protocol (MCP). It is an MCP gateway rather than a bundled-tool distribution: applications add Machai-compatible tool and prompt provider JARs to the runtime classpath, and the server discovers and publishes their capabilities to connected MCP clients. The single entry point supports local STDIO transport for desktop clients and HTTP transport for remote clients. HTTP mode can be stateless or streamable and session-oriented. The server provides command-line configuration for its advertised name, version, project-directory execution context, properties file, HTTP port, and transport mode. It is useful for AI assistant, developer automation, internal platform, and GenAI orchestration deployments that need to make independently packaged Java tools available through a standard MCP interface.",
  "authors": [
    {
      "name": "Viktor Tovstyi",
      "email": "viktor.tovstyi@gmail.com",
      "website": "https://machanism.org"
    },
    {
      "name": "Machanism.org",
      "website": "https://machanism.org"
    }
  ],
  "license": "Apache License, Version 2.0",
  "classification": {
    "type": "application",
    "domains": [
      "Model Context Protocol",
      "Generative AI",
      "AI agent tool integration",
      "Developer automation",
      "Tool orchestration"
    ],
    "languages": [
      {
        "name": "Java",
        "version": "17"
      }
    ],
    "layers": [
      "Adapters",
      "Infrastructure"
    ],
    "usageContext": [
      "MCP-compatible AI clients",
      "Command-line server deployment",
      "Local STDIO integration",
      "Remote HTTP integration",
      "Machai functional tool publication"
    ],
    "targetEnvironment": [
      "Java"
    ],
    "integrations": [
      "Model Context Protocol"
    ]
  },
  "constructors": [
    {
      "package": "org.machanism.machai.mcp.server",
      "signature": "public org.machanism.machai.mcp.server.McpServer()",
      "description": "Creates the MCP server application entry-point object. Normal deployments do not need to instantiate this class directly; they install the packaged server and invoke the fully qualified main class with the server and functional-tool provider JARs on the runtime classpath.",
      "examples": [
        {
          "snippet": "org.machanism.machai.mcp.server.McpServer server = new org.machanism.machai.mcp.server.McpServer();",
          "description": "Constructs the entry-point class directly for embedding or test setup. For practical operation, first install or build the server JAR, add a Machai-compatible functional-tool provider JAR, configure provider credentials and environment variables, and invoke org.machanism.machai.mcp.server.McpServer.main(java.lang.String[]) or run the equivalent Java command shown in the general examples."
        }
      ]
    }
  ],
  "features": [
    {
      "package": "org.machanism.machai.mcp.server",
      "name": "main",
      "signature": "public static void main(java.lang.String[] args) throws java.lang.Exception",
      "description": "Bootstraps the MCP server from command-line options. Without a port it starts an STDIO server. With --port it starts an HTTP server; combining --port with --session selects streamable HTTP transport, while HTTP without --session uses stateless transport. The server name, reported version, project directory, and properties configuration can also be supplied at startup.",
      "examples": [
        {
          "snippet": "java -cp machai-mcp-server.jar;functional-tool-container.jar org.machanism.machai.mcp.server.McpServer --projectDir C:\\work\\my-project",
          "description": "Install or build the server JAR, place it and a Machai-compatible functional-tool provider JAR on the Windows classpath, then run this command to expose discovered tools through STDIO. Configure an MCP desktop client to launch this command. On Unix-like systems, use ':' instead of ';' as the classpath separator."
        },
        {
          "snippet": "java -cp machai-mcp-server.jar;functional-tool-container.jar org.machanism.machai.mcp.server.McpServer --name team-tools --version 1.0.0 --projectDir C:\\work\\my-project --port 45000 --session",
          "description": "Run a remote, streamable MCP endpoint. First install the server and provider JARs, configure the provider's required credentials or environment settings, then invoke this command. Connect an MCP client to http://localhost:45000/mcp."
        }
      ]
    },
    {
      "package": "org.machanism.machai.mcp.server",
      "name": "getConfigurator",
      "signature": "public static org.machanism.macha.core.commons.configurator.PropertiesConfigurator getConfigurator(java.lang.String configFile) throws java.io.IOException",
      "description": "Creates the properties configurator used during server startup. A null configuration path selects mcp.properties. The absence of that default file is tolerated, whereas an explicitly requested configuration file that cannot be loaded causes an IOException.",
      "examples": [
        {
          "snippet": "java -cp machai-mcp-server.jar;functional-tool-container.jar org.machanism.machai.mcp.server.McpServer --config C:\\config\\mcp.properties --port 45000",
          "description": "Create and maintain a readable properties file for deployment-specific settings, then pass its path with --config when starting an HTTP server. This makes the chosen configuration explicit and causes startup to fail if the requested file is unavailable."
        }
      ]
    }
  ],
  "examples": [
    {
      "title": "Build and run a local STDIO MCP server",
      "steps": [
        "Install Java 17 or newer and Maven.",
        "Build from source with mvn clean package, or download a release JAR.",
        "Obtain a Machai-compatible functional tool or prompt provider JAR and configure its required credentials and environment settings.",
        "Put the server JAR and provider JAR on the Java classpath and invoke org.machanism.machai.mcp.server.McpServer without --port.",
        "Configure an MCP-compatible desktop client to launch the same Java command, then verify that the provider's tools and prompts are visible."
      ],
      "command": "java -cp machai-mcp-server.jar;functional-tool-container.jar org.machanism.machai.mcp.server.McpServer --projectDir C:\\work\\my-project"
    },
    {
      "title": "Deploy an HTTP MCP endpoint",
      "steps": [
        "Install the Machai MCP Server JAR and the required functional tool provider JARs.",
        "Configure provider-specific environment variables, credentials, models, and services before starting Java.",
        "Optionally create an mcp.properties file and supply it with --config.",
        "Start the server with --port; add --session when the MCP client requires streamable sessions.",
        "Configure the MCP client or an MCP remote proxy to use http://localhost:45000/mcp and invoke the discovered tools."
      ],
      "command": "java -cp machai-mcp-server.jar;functional-tool-container.jar org.machanism.machai.mcp.server.McpServer --config C:\\config\\mcp.properties --port 45000 --session"
    }
  ],
  "dependencies": [
    "io.modelcontextprotocol.sdk:mcp:2.0.0-RC1",
    "org.machanism.machai:genai-client:1.4.1",
    "commons-cli:commons-cli:1.11.0",
    "org.apache.commons:commons-text:1.15.0",
    "org.slf4j:slf4j-api:2.0.17",
    "org.slf4j:slf4j-simple:2.0.17",
    "org.eclipse.jetty:jetty-server:12.1.9",
    "org.eclipse.jetty.ee10:jetty-ee10-servlet:12.1.9"
  ]
}
